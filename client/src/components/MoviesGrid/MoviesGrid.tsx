import { Box, Grid, Typography } from "@mui/material";
import MovieCard from "../MovieCard/MovieCard";


const MovieGrid = () => {

  return (
    <Grid container sx={{ justifyContent: "flex-start", width: "80vw", border: "1px solid white" }}>
    </Grid>
  );
};

export default MovieGrid;
